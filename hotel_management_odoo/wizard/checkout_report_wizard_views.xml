<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Check Out Report Wizard Form View -->
    <record id="checkout_report_wizard_view_form" model="ir.ui.view">
        <field name="name">checkout.report.wizard.view.form</field>
        <field name="model">checkout.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Check Out Report">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group>
                        <field name="partner_id" options="{'no_create': True, 'no_open': True}"/>
                        <field name="room_id" options="{'no_create': True, 'no_open': True}"/>
                    </group>
                </group>
                <footer>
                    <button name="action_print_pdf" string="Download PDF" type="object" class="btn-primary" icon="fa-download"/>
                    <button name="action_print_excel" string="Download XLS" type="object" class="btn-primary" icon="fa-download"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Check Out Report Wizard Action -->
    <record id="checkout_report_wizard_action" model="ir.actions.act_window">
        <field name="name">Check Out Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">checkout.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
