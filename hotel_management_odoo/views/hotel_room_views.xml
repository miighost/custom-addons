<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Hotel Room list view (Sorted by Name with Badges) -->
    <record id="hotel_room_view_tree" model="ir.ui.view">
        <field name="name">hotel.room.view.tree</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <list default_order="name asc" decoration-muted="not active">
                <field name="name" string="Room No / Name" decoration-bf="1"/>
                <field name="room_type" string="Room Type"/>
                <field name="floor_id" string="Floor"/>
                <field name="num_person" string="Capacity (Pax)"/>
                <field name="list_price" string="Rent / Day" widget="monetary"/>
                <field name="status" widget="badge"
                       decoration-success="status == 'available'"
                       decoration-warning="status == 'reserved'"
                       decoration-danger="status == 'occupied'"/>
                <field name="active" column_invisible="True"/>
            </list>
        </field>
    </record>

    <!-- Hotel Room search view -->
    <record id="hotel_room_view_search" model="ir.ui.view">
        <field name="name">hotel.room.view.search</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <search string="Search Rooms">
                <field name="name" string="Room Name / Number"/>
                <field name="floor_id" string="Floor"/>
                <field name="room_type" string="Room Type"/>
                <separator/>
                <filter string="Available Rooms" name="available" domain="[('status', '=', 'available')]"/>
                <filter string="Reserved Rooms" name="reserved" domain="[('status', '=', 'reserved')]"/>
                <filter string="Occupied Rooms" name="occupied" domain="[('status', '=', 'occupied')]"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Floor" name="group_by_floor" context="{'group_by': 'floor_id'}"/>
                    <filter string="Room Type" name="group_by_room_type" context="{'group_by': 'room_type'}"/>
                    <filter string="Status" name="group_by_status" context="{'group_by': 'status'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Hotel Room form view -->
    <record id="hotel_room_view_form" model="ir.ui.view">
        <field name="name">hotel.room.view.form</field>
        <field name="model">hotel.room</field>
        <field name="arch" type="xml">
            <form string="Hotel Room">
                <header>
                    <field name="status" widget="statusbar" readonly="1"/>
                </header>
                <sheet>
                    <field name="room_image" widget="image"
                           class="oe_avatar"
                           options="{'preview_image': 'room_image'}"/>
                    <div class="oe_title">
                        <label for="name" string="Name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <h2>
                            <field name="is_room_avail" invisible="1"/>
                        </h2>
                    </div>
                    <notebook>
                        <page name="information_hotel_room"
                              string="Information">
                            <group>
                                <group>
                                    <field name="floor_id" string="Floor"/>
                                    <field name="user_id" string="Room Manager"/>
                                    <field name="taxes_ids"
                                           widget="many2many_tags"
                                           string="Customer Taxes"/>
                                </group>
                                <group>
                                    <field name="room_type"/>
                                    <field name="list_price" widget="monetary"
                                           string="Rent"/>
                                    <field name="num_person"/>
                                    <field name="uom_id" invisible="1"/>
                                </group>
                            </group>
                        </page>
                        <page string="Amenities">
                            <group>
                                <field name="room_amenities_ids"
                                       widget="many2many_tags"
                                       string="Room Amenities"/>
                            </group>
                        </page>
                        <page string="Descriptions">
                            <separator string="Description"/>
                            <field name="description" colspan="4" nolabel="1"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Hotel Room action -->
    <record id="hotel_room_action" model="ir.actions.act_window">
        <field name="name">Rooms</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hotel.room</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="hotel_room_view_search"/>
        <field name="context">{'default_is_room_avail': True}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                No Rooms found! Let's create one.
            </p>
        </field>
    </record>

    <!-- Direct Rooms Menu on Main Navbar -->
    <menuitem id="hotel_room_main_menu"
              name="Rooms"
              action="hotel_room_action"
              parent="hotel_management_menu_root"
              sequence="15"/>

    <!-- Room Menu under Configuration -->
    <menuitem id="hotel_room_menu"
              name="Room"
              action="hotel_room_action"
              parent="hotel_config_menu"
              sequence="20"/>
</odoo>
